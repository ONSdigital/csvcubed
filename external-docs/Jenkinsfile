pipeline {
    agent {
        dockerfile { 
            reuseNode true
        }
    }
    stages {
        stage('Documentation') {
            steps {
                script {
                        dir ('external-docs'){
                            sh 'bundle install'
                            sh 'bundle exec middleman build'
                        }
                        stash name: 'docs', includes: '**/docs/_build/html/**/*'
                }
            }
        }
    }
}